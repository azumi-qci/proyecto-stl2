import { FC } from 'react';
import TextEditor from './components/TextEditor';

const Layout: FC = () => {
  return (
    <div>
      <TextEditor />
      <p>Layout</p>
    </div>
  );
};

export default Layout;
